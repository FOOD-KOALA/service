server.port=8080
spring.application.name=prod-eng-service
mongodb.connection.url=${MONGODB_CONECTION_URL:mongodb://root:example@localhost:27017}
management.endpoint.prometheus.enabled=true
management.endpoints.web.exposure.include=info,health,prometheus
echo "management.endpoint.prometheus.enabled=true
management.endpoints.web.exposure.include=info,health,prometheus" >> /workspaces/service/src/main/resources/application.properties
